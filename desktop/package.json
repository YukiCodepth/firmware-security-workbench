{
  "name": "firmware-security-workbench-desktop",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "prebuild": "tauri icon app-icon.svg",
    "build": "tauri build",
    "icon": "tauri icon app-icon.svg",
    "preview": "python3 -m http.server 4173 --directory app"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}

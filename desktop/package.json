{
  "name": "firmware-security-workbench-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "preview": "python3 -m http.server 4173 --directory app"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
